<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Author - Biraj</title>
  <style>
    /* Author Section Styling */
    .author {
      padding: 40px 20px;
      text-align: center;
      background-color: #f7f9fc;
    }

    .author_heading {
      font-size: 2.5rem;
      color: #2c3e50;
      margin-bottom: 10px;
    }

    .author_body {
      font-size: 1.25rem;
      color: #7e7e7e;
      margin-bottom: 20px;
    }

    .hero-image {
      width: 100%;
      height: auto;
      max-width: 980px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      margin-bottom: 40px;
    }

    .articles__heading {
      font-size: 2rem;
      color: #2c3e50;
      margin-bottom: 20px;
      text-align: center;
    }

    .article-ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    .article-ul li {
      margin: 10px 0;
      border-bottom: 1px solid #e4e4e4;
      padding-bottom: 10px;
    }

    .article-ul li a {
      color: #2c3e50;
      text-decoration: none;
      font-size: 1.1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .article-ul li a:hover {
      color: #007bff;
      text-decoration: underline;
    }

    .article-title {
      font-weight: bold;
    }

    .article-list__date {
      font-size: 0.9rem;
      color: #7e7e7e;
    }

    .pagination {
      display: block;
      text-align: center;
      margin-top: 30px;
      font-size: 1.1rem;
      color: #007bff;
      text-decoration: none;
    }

    .pagination:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="author">
    <h1 class="author_heading">Hi, I am Biraj.</h1>
    <p class="author_body">Web Developer and AI Enthusiast.</p>
    <img src="https://miro.medium.com/v2/resize:fit:1400/0*SjQDh1Z96unvQMid" alt="Author Image" class="hero-image" width="981" height="528">

    <section class="articles">
        <h2 class="articles__heading">Latest Posts</h2>
      
        <ul class="article-ul">
          <% data.forEach(post => { %>
            <li>
              <a href="/post/<%= post._id %>">
                <span class="article-title"><%= post.title %></span>
                <span class="article-list__date"><%= post.createdAt.toDateString() %></span>
              </a>
            </li>
          <% }) %>
        </ul>
      
        <% if (nextPage !== null) { %>
            <a href="/?page=<%= nextPage %>" class="pagination">&lt; View Older Posts</a>
        <% } %>
    </section>
  </div>

</body>
</html>
